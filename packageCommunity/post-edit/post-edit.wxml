<view class="top">
  <view class="header" style="padding-top:{{navBarHeight-44}}px;padding-left:{{menuRight+20}}rpx">
    <view class="header-left" bindtap="goBack">
      <t-icon name="chevron-left" size="40rpx" color="#2d3436" />
    </view>
    <text class="title">{{editMode ? '编辑帖子' : '发帖'}}</text>
    <view class="action-group" style="right:{{menuRight + menuWidth + 10}}px">
      <button class="publish-btn" disabled="{{!canPublish}}" loading="{{submitting}}" bindtap="handleSubmit">
        {{editMode ? '保存' : '发布'}}
      </button>
    </view>
  </view>
</view>

<view class="edit-container">
  <view class="user-row">
    <image class="user-avatar" src="{{userInfo.avatar || '/images/avatar.png'}}" mode="aspectFill" binderror="onAvatarError" />
    <text class="user-nickname">{{userInfo.nickname || '用户'}}</text>
  </view>

  <textarea
    class="content-textarea"
    placeholder="分享你的想法..."
    value="{{content}}"
    bind:input="onContentInput"
    maxlength="2000"
    auto-height
    show-confirm-bar="{{false}}"
  />

  <view class="upload-section">
    <t-upload
      mediaType="{{['image']}}"
      class="image-upload"
      source="{{imageSource}}"
      max="{{9}}"
      files="{{fileList}}"
      gridConfig="{{gridConfig}}"
      config="{{uploadConfig}}"
      draggable="{{true}}"
      bind:add="handleImageAdd"
      bind:remove="handleImageRemove"
      bind:click="handleImageClick"
    />
    <view class="image-source-switch" wx:if="{{fileList.length < 9}}">
      <text class="switch-label">从聊天记录选择</text>
      <t-switch value="{{imageSource === 'messageFile'}}" bind:change="toggleImageSource" size="small" />
    </view>
  </view>

  <view class="location-section" wx:if="{{!location}}" bind:tap="pickLocation">
    <t-icon name="location" size="32rpx" color="#999" />
    <text class="location-placeholder">添加位置</text>
  </view>
  <view wx:if="{{location}}" class="location-section location-selected">
    <t-icon name="location" size="32rpx" color="#00b26a" />
    <text class="location-text">{{location.text}}</text>
    <view class="location-clear" catch:tap="clearLocation">
      <t-icon name="close" size="28rpx" color="#999" />
    </view>
  </view>

  <view class="word-count">
    <text>{{content.length}}/2000</text>
    <text wx:if="{{!title && content.length > 0}}" class="hint">首行将作为标题</text>
  </view>
</view>
